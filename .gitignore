# No trailing slash: also ignores a stray symlink named node_modules,
# which a directory-only pattern would let through.
node_modules
dist-v2/
playwright-report/
test-results/
.vite/
.env
.env.*
!.env.example
.DS_Store
Thumbs.db

# impeccable design-review tool: platform binary is fetched by npx, not committed
.claude/skills/impeccable/scripts/bin/
.github/skills/impeccable/scripts/bin/

.wrangler-accounts-*.jsonc

# Local wrangler dev state (miniflare databases and caches)
.wrangler/
